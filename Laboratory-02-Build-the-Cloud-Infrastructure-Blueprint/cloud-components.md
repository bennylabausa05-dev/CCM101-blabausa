### Compute Resources
Some of the compute resources found is the CPU, Storage, Disk, and also its own Operating System which is the Ubuntu 24.04, it is very important that it have these all basic components to run and do some of basic task of a computer.

### Storage Resources
On the storage resources although it doesn't have that much of a capacity I think it will do the job of basic command and other stuff.

### Networking Resources
On the Networking Resources i found out that when prompting you can check only ip address that is public only inro the webserver and can also be specifically display IP addresses that are more privately that is not allowed for sharing.

### Operating System
On operating system we used the Ubuntu 24.04 version because that what inside the killercoda is available for, because it is running on a virtual machine i think when typing a command into the Linux Terminal it generate or display information that is identical as a real Linux Environment, Because it is Used for Testing and Learning. 
